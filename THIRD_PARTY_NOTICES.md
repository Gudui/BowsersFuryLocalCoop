# Third-party notices

The public build uses the pinned ExLaunch revision declared in `.gitmodules` and `release/public-manifest.json`. ExLaunch's license and notices are available in the checked-out `vendor/exlaunch` dependency.

The release package contains only the mod module, generated NPDM, and settings file. It does not contain Nintendo game content, keys, or executable dumps.
